"use client";
import React from "react";

export default function RecruiterOverview() {
  // const res = await new Promise((resolve) => setTimeout(resolve, 2000));
  // console.log("Overview component rendered", res);
  return <div>overview</div>;
}
